# Copy this file to `.env` and fill in your key.
# `.env` is gitignored. Bun auto-loads it when you run `bun src/cli.ts ...`.

# Required for `bun src/cli.ts run "<prompt>"`. Get a key at
# https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
